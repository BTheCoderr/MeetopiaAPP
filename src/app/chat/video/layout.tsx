import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Video Chat — Meetopia',
}

export default function VideoChatLayout({ children }: { children: React.ReactNode }) {
  return children
}
